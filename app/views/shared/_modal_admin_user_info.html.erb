<div class="reveal modal_general_design" id="<%= dom_id(user) %>_user_content" data-reveal>
  <div class="modal_header"><%= "#{user.firstname} #{user.lastname}" %></div>
  <div class="modal_content">
    <div><%# @smallgroup.descriptin %></div>
  </div>
  <button class="close-button" data-close aria-label="Close modal" type="button">
    <span aria-hidden="true">&times;</span>
  </button>
</div>